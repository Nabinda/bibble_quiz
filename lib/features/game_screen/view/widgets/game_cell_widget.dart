import 'package:flutter/material.dart';

class GameCellWidget extends StatelessWidget {
  const GameCellWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
